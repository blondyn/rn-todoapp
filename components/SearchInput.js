import React from 'react';
import {TextInput} from 'react-native';

export default class SearchInput extends React.Component {
    render = () => (<TextInput placeholder={'Enter an item!'}></TextInput>)
}